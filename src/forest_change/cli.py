"""Command-line entry point.

Once implemented (later phases), this will support:

    python -m forest_change.cli run --config config/demo_site.yaml

For now this is a Phase 0 placeholder so the package installs
cleanly and the `forest-change` command actually exists, even though
it doesn't do anything useful yet.
"""


def main() -> None:
    print("forest-change-monitor: not implemented yet (Phase 0 scaffold).")


if __name__ == "__main__":
    main()
