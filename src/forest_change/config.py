"""Load and validate config/demo_site.yaml.

Phase 0 placeholder. Will eventually turn the YAML file into a small,
typed Python object (e.g. a dataclass) so the rest of the code never
has to touch raw YAML/dict access directly.
"""
