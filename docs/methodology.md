# Methodology mapping: this project vs. Equitable Earth's real workflow

*(Placeholder - to be filled in once the pipeline produces real
output, so the comparison has something concrete to point at.)*

This will lay out, side by side:

- What Equitable Earth's real certification workflow does, per its
  public Standard and M002 (Terrestrial Forest Conservation / AUDD)
  methodology documents - centralized, jurisdiction-level baselines,
  pixel-level above-ground biomass modeling, ESA WorldCover-based
  stratification, and continuous digital MRV.
- What this small educational project does instead - a single-pair
  NDVI comparison over one AOI, using greenness as a rough proxy.
- Exactly where the two diverge, and why that gap matters.
